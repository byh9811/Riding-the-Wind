<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ringdingdong.ridingthewind.attraction.entity.AttractionInfo">

    <resultMap type="attractionInfo" id="attractionInfo">
        <result column="content_id" property="contentId"/>
        <result column="content_type_id" property="contentTypeId"/>
        <result column="title" property="title"/>
        <result column="addr1" property="addr1"/>
        <result column="addr2" property="addr2"/>
        <result column="zipcode" property="zipcode"/>
        <result column="tel" property="tel"/>
        <result column="first_image" property="firstImage"/>
        <result column="sido_code" property="sidoCode"/>
        <result column="gugun_code" property="gugunCode"/>
        <result column="latitude" property="latitude"/>
        <result column="longitude" property="longitude"/>
    </resultMap>

    <select id="selectAttractionListBySidoAndGugun" parameterType="map" resultMap="attractionInfo">
        select *
        from attraction_info
        where sido_code = #{sidoCode} and gugun_code = #{gugunCode}
    </select>

    <select id="selectAttractionListByUserId" parameterType="String" resultMap="attractionInfo">
        select *
        from attraction_info
        natural join personal_trip
        where userId = #{userId}
    </select>

</mapper>